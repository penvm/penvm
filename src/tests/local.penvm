name: local
description:

networks:
  default:
    targets: localhost
  locals:
    targets: local-[0-3]

templates:
  locals:
    targets: local-[0-3]
    scheme: local
    host: localhost

targets:
  localhost:
    scheme: local
